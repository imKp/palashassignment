//This information will be required throught the session
//The id for user is stored to maintain reference of all activities done by user
class UserData{
  String titleSearched;
  String userId;
  UserData({
    required this.titleSearched,
    required this.userId
  }); 
}

